#include "main.h"

/**
 * print_char - Adds a character to the buffer
 * @c: Character to be printed
 * @count: Pointer to the character count
 */
void print_char(char c, int *count)
{
    add_to_buffer(c, count);
}